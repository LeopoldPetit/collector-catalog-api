# Copie ce fichier en .env - ne jamais committer de vraies valeurs (cible finale = Vault)
DATABASE_URL="postgresql://collector:collector_dev_password@localhost:5432/collector?schema=catalog"
PORT=3000
